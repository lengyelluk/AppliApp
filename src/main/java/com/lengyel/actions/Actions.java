package com.lengyel.actions;

import com.lengyel.web.GuiElement;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.openqa.selenium.By;


public class Actions {

    private static final Logger logger = LogManager.getLogger(Actions.class);


   /* public void loadUrl(String url) {
        logger.info("loading url: " + url);
        getDriver().get(url);
        getDriver().manage().window().maximize();
    }

    public List<WebElement> getAllMatchingElements(By by) {
        return getDriver().findElements(By.name("name"));
    }*/


}
